import { Text, View } from "react-native";

// Placeholder — el contenido real de Home (mapa + órdenes activas) llega en su propio change de OpenSpec.
export default function Home() {
  return (
    <View className="flex-1 items-center justify-center bg-white">
      <Text className="text-primary text-xl font-bold">Próximamente</Text>
    </View>
  );
}
